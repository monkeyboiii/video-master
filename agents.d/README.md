# agents.d

What an agent needs about video-master, in one place:

- `memory/` — facts true only for this repo; read `MEMORY.md` first, then only what applies.
- `modules/` — the docs (`kind: why | runbook | spec | note`, frontmatter on each).
- `skills/` — repo-local skills, linked into the harness by `dbx skills sync`.

Shape, rules and rollout: `dirtbikex-agent-coding/playbook/agents-d.md`. Migrated 2026-09-07.
