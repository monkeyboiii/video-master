# video-master memory index

Facts true **only** for this repo. Lazily loaded: read this index, then open only the fact you
need. One line per fact; the fact itself lives in its own file with frontmatter
(name, description, metadata.type).

(none recorded yet)
