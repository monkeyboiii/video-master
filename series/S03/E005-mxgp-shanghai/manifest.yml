# Episode manifest — source of truth for this episode.
# Schema notes and naming: agents.d/modules/naming-conventions.md
# Validate with: tools/vm check

video_id: "S03E005"
slug: "mxgp-shanghai"
# The cut arrived already edited and exported (media/exports/S03E005-cn.MP4) — shooting and
# editing both happened outside this repo, the way toolline.md § "The cut comes from outside"
# describes. What landed here so far is the captions job: a whisper.cpp/DTW transcription of
# the real take, grouped into words against the operator's own transcript. No beats table is
# authored — status stays "topic" on purpose rather than claiming "scripting", which would
# require one (tools/series/episode.mjs); backfilling beats, a cover and platform assignment
# is a separate pass for whoever picks up full onboarding.
status: topic

formats:
  primary: short_vertical
  aspect: 9x16
  resolution: 1080x1920
  fps: 30

variants:
  zh-CN:
    script: script.zh-CN.md
    cover: null
    subtitles: null
    # v3 is the operator's final pick: emoji (🥚, Apple Color Emoji — see fonts.ts) for the
    # crotch line, plus hand-tuned timing for two words whisper never anchored (What; 骑).
    # v1 (text) and v2 (first emoji pass, wrong word + short "What") are retired.
    captions: remotion-props/spoken-captions.zh-CN.v3.json
    remotion_props: null
    voiceover_asset_id: null
    platforms: [rednote, douyin, bilibili]
